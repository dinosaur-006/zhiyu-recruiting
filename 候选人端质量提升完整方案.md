# 候选人端质量提升完整方案

> 诊断日期: 2026-06-12 | 目标: Demo → 真正产品

---

## 核心诊断：为什么感觉不像产品？

经逐文件审计（6 页面 + 6 核心组件 + 2 Store），当前 P0-P3 的交互骨架已搭建完整——地图弹簧散射、流式对话、报告编辑排版——但**所有内容都是假数据**。候选人看到的不是"这个岗位的真实信息"，而是写死的字符串。

真正产品的核心特征是**数据驱动**：不同岗位展示不同内容，不同候选人看到不同反馈。当前的问题不是 UI 不够好，是**数据链路没跑通**。

---

## 完整问题清单（按严重程度排序）

### 🔴 P0 — 功能断裂（必须修）

#### 1. ExplorePanel 情报卡片全部是写死的

**当前**：`ExplorePanel.tsx` 第 3-12 行定义 `DB: Record<string, {title, content, badges}>`，8 条硬编码文本，不随岗位变化。

**问题**：候选人探索"前端工程师"和"运营专员"看到完全一样的情报卡片。ExplorePanel 与 `demoStore` 零连接——它不读取 `state.jobTruthLabels`，不利用 `state.job` 的任何数据。

**修复**：删除 `DB` 常量。`ExplorePanel` 改为从 `useDemoState()` 读取 `state.jobTruthLabels.find(l => l.jobId === jobId)`，动态生成每个节点对应的工作节奏、协作密度、成长速度等真实数据。

**文件**：`ExplorePanel.tsx`（约 50 行改动）

#### 2. Profile 页面未接入异步轮询

**当前**：`Profile.tsx` 调用 `submitCandidateApplicationWithAi()`——这是 demoStore 中的同步 19 步链式生成（约 2 秒内完成），不经过 `useReportGeneration` 钩子，不走 `/api/ai/submit-trial` 异步队列。

**问题**：后端已部署异步任务队列（`taskRegistry` + `runAsyncReportGeneration`），但前端从未调用。Profile 提交后秒跳 StoryPreview，用户看不到任何进度反馈，也没有利用真正的 DeepSeek API 生成报告。

**修复**：`Profile.tsx` 的 `submit` 函数改为先调用 `/api/ai/submit-trial` 获取 `taskId`，然后使用 `useReportGeneration` 钩子轮询进度，加载中显示 `progressText`，完成后自动跳转。

**文件**：`Profile.tsx`（约 15 行改动）

#### 3. 云试岗场景全部是固定剧本

**当前**：`Chat.tsx` 使用 `startTrialSession` → `state.realityScenes` 读取 3 个预生成场景（intro/dayInLife/taskChallenge），每个场景的脚本在 HR 创建岗位时由 `generateRealityScripts()` 一次性生成——这是 Mock 函数，基于关键词匹配。

**问题**：无论候选人是谁、关注什么、之前做了什么选择，场景脚本都是固定不变的。没有利用 DeepSeek 的 `streamDeepSeekChat` 实时生成个性化场景。分支选择的"AI 回复"虽然调用了真实 API，但场景本身是死的。

**修复**：`Chat.tsx` 在进入第三幕（taskChallenge）时，调用 `streamAiResponse(jobContext)` 让 DeepSeek 动态生成分岔任务描述，不再使用预生成脚本。前两幕（intro/dayInLife）可从岗位真相标签自动生成文案。

**文件**：`Chat.tsx` + `server/prompts/generateScenarioPrompt.ts`（已存在，未使用）

---

### 🟡 P1 — 体验打磨（影响产品感）

#### 4. 移动端证据按钮使用 `onMouseEnter`

**当前**：`StoryPreview.tsx` 第 59 行使用 `onMouseEnter` + `onMouseLeave` 调用 `setActiveEvidenceId()` 触发右侧 ReviewPanel 切换。

**问题**：移动端没有鼠标悬停事件，证据按钮在手机上完全无效。触屏用户看不到"高光行为回放"。

**修复**：改为 `onClick`（点击切换高亮状态，再点取消），同时在触屏设备上保持可用。增加 `aria-pressed` 属性提升无障碍。

**文件**：`StoryPreview.tsx`（约 5 行改动）

#### 5. 页面间路由切换无过渡动画

**当前**：React Router 的页面切换是即时替换——JobDetail → Chat → Profile → StoryPreview → Success 之间没有过渡效果。

**问题**：5 个页面的割裂感——候选人不觉得自己在经历一段连续旅程，而是"跳转到了另一个页面"。

**修复**：在 `CandidateLayout` 的 `<Outlet />` 外层包裹 `framer-motion` 的 `<AnimatePresence mode="wait">`，每个页面自动获得 `fadeInUp` 入场 + `fadeOut` 退场动画。零额外代码，仅 CSS 过渡。

**文件**：`CandidateLayout.tsx`（约 10 行改动）

#### 6. 数字人初始化消息永远是同一个

**当前**：`chatStore.ts` 第 30-37 行硬编码了 `msg-init`：`'你好！我是这个岗位的AI体验官。接下来我们将进入前端开发的日常工作沙盘...'`

**问题**：无论候选人进入哪个岗位的云试岗，第一条消息都是"前端开发"。产品专员进入也看到前端开发的描述。

**修复**：`chatStore` 增加 `resetWithJob(jobTitle)` 方法，根据当前岗位动态生成第一条欢迎消息。`Chat.tsx` 在 `useEffect` 中调用。

**文件**：`chatStore.ts` + `Chat.tsx`（约 10 行改动）

---

### 🟢 P2 — 招聘痛点未解决

#### 7. HR 无法感知候选人进度

**当前**：HR 端与候选人端之间的数据同步仅靠 `demoStore` 的 localStorage。候选人完成某个场景、提交资料——这些事件不会主动通知 HR 端。

**问题**：真实的招聘场景中，HR 想知道"候选人是否开始了云试岗？""进展到第几幕了？""为什么中途退出？"当前系统无法回答这些问题。

**修复**：`Chat.tsx` 的 `completeRealityScene`、`selectBranchChoice`、`recordCandidateExitReason` 在调用 demoStore 动作后，同步写入 `state.metrics` 的实时更新字段（`latestCandidateActivity`）。HR 端 Dashboard 显示"候选人林同学 刚刚完成第二幕"。

**文件**：`demoStore.ts`（+`latestCandidateActivity` 字段）+ `Dashboard.tsx`（+实时活动面板）（约 30 行改动）

#### 8. AI 洞察报告没有体现"为什么"

**当前**：`StoryPreview.tsx` 的能力亮点只有描述文字（如"主动推进复杂任务的能力"），没有说明这个结论是怎么得出的。

**问题**：候选人看到的是一份"AI 说我很厉害"的报告，但不知道 AI 为什么这么说。这正是招聘中的核心痛点——AI 黑箱。如果不解决，候选人不会信任这份报告。

**修复**：`StoryPreview` 的能力亮点卡片中，增加"证据锚点"——点击展开后显示一两句来自云试岗对话的原文摘录（从 `chatStore.chatHistory` 中提取）。例如："因为你在情景中选择 Mock 先行方案..." → 旁边显示当时对话截图。

**文件**：`StoryPreview.tsx` + `chatStore.ts`（+`getEvidenceAnchor()` 方法）

#### 9. 没有"双向匹配"的感觉

**当前**：整个体验是单向的——候选人了解岗位、展示能力、获得评分。候选人没有机会表达"我想要什么"。

**问题**：真正的双向匹配应该是——"这是你的偏好，这是岗位的实际情况，这里是匹配点，这里是摩擦点"。目前只有"岗位→候选人"的单箭头。

**修复**：`StoryPreview.tsx` 增加一个"你的偏好 vs 岗位实际"的对比卡片。从 `candidate.profile` 提取关注点（concerns, motivation），与 `job.truthLabel` 的真实数据进行可视化对比（如"你重视成长→岗位成长速度为高→匹配"）。

**文件**：`StoryPreview.tsx` + `Success.tsx`（约 30 行改动）

---

## 实施路线图

| 优先级 | 问题 | 改动量 | 预估时间 | 效果 |
|--------|------|--------|---------|------|
| P0-1 | ExplorePanel 动态数据 | ~50 行 | 30 分钟 | 不同岗位展示不同情报 |
| P0-2 | Profile 异步轮询 | ~15 行 | 15 分钟 | 提交后进度反馈 |
| P0-3 | 云试岗场景 AI 生成 | ~20 行 | 20 分钟 | 场景不再写死 |
| P1-4 | 移动端证据按钮 | ~5 行 | 5 分钟 | 手机上可用 |
| P1-5 | 页面过渡动画 | ~10 行 | 10 分钟 | 旅程连续感 |
| P1-6 | 数字人动态欢迎语 | ~10 行 | 10 分钟 | 不同岗位不同开场 |
| P2-7 | HR 实时活动感知 | ~30 行 | 30 分钟 | 招聘闭环 |
| P2-8 | 证据锚点可解释 | ~20 行 | 20 分钟 | AI 可信度 |
| P2-9 | 双向匹配可视化 | ~30 行 | 30 分钟 | 匹配感 |

**总计：约 200 行改动，3 小时内可全部完成。**

---

## 不改的（已经正确）

以下功能经代码级审计确认完整无误，无需改动：

| 功能 | 状态 | 证据 |
|------|------|------|
| JobExplorationMap 弹簧散射 | ✅ | framer-motion Spring + 极坐标，`setExploreNodeId` 联动 ExplorePanel |
| ChatPanel 流式打字 | ✅ | SSE + ReadableStream + activeStreamContent，自动降级 Mock |
| ReviewPanel 证据回放 | ✅ | AnimatePresence + `activeEvidenceId` 联动 StoryPreview 悬停 |
| Success 五彩纸屑 | ✅ | Framer Motion 30 粒子 + 重力缓动 + 颜色交错 |
| CopilotSidebar 面板切换 | ✅ | route-derived mode + visibility + pointer-events fix |
| SSE 流式引擎 | ✅ | DeepSeek `stream:true` + AsyncGenerator + buffer 防截断 |
| 异步任务队列 | ✅ | taskRegistry + POST submit-trial + GET task/:id 轮询 |
| 数据隔离 | ✅ | `resetDemoState()` 同时清除 localStorage + IndexedDB |
| Dashboard Dark Editorial | ✅ | CSS Modules + Radar + 热力图 + KPI 卡片 |


# Talent Sandbox 3.0 — 前端深度分析报告

> 日期: 2026-06-12 | 范围: 候选人端 + HR 端 全页面

---

## 一、总体评估

### 核心问题

当前前端**不像一个真正的产品**。原因：

1. **视觉语言碎片化**: 暗黑→清新转换不彻底，270+ 硬编码色散布 23 个文件
2. **页面体验割裂**: Home / Sandbox / Chat / HR 四个区域各自为政，无统一设计语言
3. **交互范式混杂**: 拖拽 + 点击 + 终端指令 + 聊天气泡同时存在，用户心智模型混乱
4. **布局臃肿**: Timeline 侧轨 + Aura 光核 + 场景脚本 + 交互组件 = 信息过载
5. **HR 端"工具化"残留**: 表格、表单、筛选标签——仍在用传统 SaaS 范式

### 产品应有的感受

一个每月收费数千美元的专业产品应该像 **Linear** 或 **Notion** ——进入页面的第一秒就知道"这是顶级团队做的"，每个像素都有存在的理由，零视觉噪音。

---

## 二、逐页诊断

### 2.1 Home（机密任务面板）

**当前状态**: 3 张垂直卡片，TARGET_ID + 压力标签 + INFILTRATE 按钮 + 终端握手动效。

**问题**:
- 三卡等宽并排——在所有屏幕宽度下都是同一布局，无响应式
- 压力标签 `高危/适中/平稳` 与卡片内容之间缺乏视觉关联
- 终端握手动效 2.2s 延迟——Demo 场景可以，真实产品中候选人会觉得卡顿
- 卡片间距 24px 在移动端过窄
- SYS_ADMIN 后门几乎不可见——真实 HR 可能找不到入口

### 2.2 JobDetail（引力沙盘）

**当前状态**: 5 个可拖拽节点（薪资/技术/节奏/团队/成长）+ 中心引力核 + CTA。

**问题**:
- 沙盘占据全屏——没有导航、没有返回、没有任何上下文
- 节点从轨道位置开始——初始状态对新手无意义
- "拖动节点靠近中心"的提示太隐蔽
- 只有一个 demo——多个岗位时没有岗位选择器

### 2.3 Chat（三联场景）

**当前状态**: Timeline 56px 侧轨 + AuraInsightNode + IntelArchive/Blueprint/Crucible。

**问题**:
- Timeline 侧轨占用 56px——对 90% 用户无用的空间
- Aura 光核 + 消息气泡 + 洞察卡片 = 3 层信息堆叠
- IntelArchive 的涂黑条动画在 Lucid 浅色主题下几乎不可见（黑条在白底上很突兀）
- EnvironmentBlueprint 的 SVG 热点在移动端无法点击（太小）
- TacticalCrucible 拖拽在移动端完全不可用
- 三个场景之间的过渡无动画——内容突然替换

### 2.4 HR Jobs

**当前状态**: 卡片列表，技能标签，薪资范围。

**问题**:
- 信息密度低——每个岗位占 200px+ 高度
- 没有搜索/筛选——3 个岗位就全量展示
- "0 候选人"文字无引导——不明所以

### 2.5 HR JobNew（Genesis Engine）

**当前状态**: JD 粘贴区 + EXTRACT_DNA + 5 滑块 + 表单折叠。

**问题**:
- 滑块面板过大——5 个滑块 + Mini-Console 占据半屏
- EXTRACT_DNA 按钮在无 JD 时不可用但无提示
- 表单折叠区与滑块面板之间的关系不明确

### 2.6 HR Dashboard / Candidates

**当前状态**: KPI 卡片 + 图表 + 候选人列表。

**问题**:
- Dashboard 无数据时显示模板占位
- 候选人列表空状态仅一行文字
- 图表使用 Recharts 默认样式——与品牌不一致

---

## 三、架构层面问题

### 3.1 组件耦合
- `Chat.tsx` 410 行——集场景路由、消息渲染、AI 流、退出弹窗于一体
- 数字人/Aura 组件在 Chat 内硬编码——无法在其他页面复用

### 3.2 状态管理
- `chatStore` + `demoStore` 双重状态源——数据同步依赖 useEffect
- localStorage 存储全部 DemoState——刷新时数据重建逻辑复杂

### 3.3 响应式
- 全站 0 个 @media breakpoint
- 仅在 Home 卡片有 `flexWrap`
- Timeline 56px 在手机屏幕上占比过高

---

## 四、重构方向

### 核心理念: 「Swiss Precision × Japanese Warmth」

像瑞士制表般精确的间距/对齐/动效，像日式待客般温暖的色彩/留白/微交互。

### 设计原则
1. **一屏一事**: 每个视口只呈现一个核心交互
2. **渐进披露**: 先展示最重要的，细节按需展开
3. **材质优先**: 玻璃态/微阴影/柔光——用物理隐喻建立层次
4. **移动优先**: 从小屏开始设计，桌面端是增强版

详见实施方案。

# 候选人端完整代码分析报告

> 日期: 2026-06-12 | 产品: 职遇 Reality Pro

---

## 一、代码规模总览

| 类别 | 文件数 | 说明 |
|------|--------|------|
| 路由页面 | 6 | JobDetail, Chat, Profile, StoryPreview, Success, CandidateLayout |
| 核心组件 | 6 | JobExplorationMap, CopilotSidebar, ExplorePanel, ChatPanel, ReviewPanel, ComplianceNotice |
| 商店 | 2 | demoStore (业务数据), chatStore (Zustand 流式聊天) |
| 共享组件 | 26 | 面板组件(Badge/StatCard/Trust系列/报告系列等) |
| CSS | 1 | global.css (~3000行, Swiss Spa + Dark Editorial 双主题) |
| **合计** | **41** | |

---

## 二、路由与页面结构

```
/candidate
  ├── CandidateLayout.tsx          ← 布局外壳 (grid容器 + 主题切换)
  │
  ├── /job/:jobId → JobDetail.tsx   ← P2 岗位探索地图
  ├── /chat/:jobId → Chat.tsx      ← P1 云试岗
  ├── /profile/:jobId → Profile.tsx  ← 资料补充
  ├── /story/:candidateId → StoryPreview.tsx ← P3 报告预览
  └── /success/:candidateId → Success.tsx    ← 投递成功
```

### CandidateLayout.tsx (29行)
- **职责**: 全宽网格外壳 + 主题激活
- **布局**: `grid-template-columns: 1fr 400px` (桌面), `display: block` (移动)
- **左侧**: `<Outlet />` 渲染子路由
- **右侧**: `<CopilotSidebar />` 常驻数字人面板
- **主题**: `useEffect` 设置 `data-theme="candidate"` → 温暖 Swiss Spa 配色

### JobDetail.tsx (67行)
- **职责**: 岗位入口页, 承载探索地图 + 两个模态框
- **核心数据**: `state.jobs`, `state.jobTruthLabels`, `state.jobTruthContracts`
- **核心动作**: `startTrialSession()` → 导航到 Chat, `acknowledgeTruthContract()`
- **模态框1**: AI 身份披露弹窗 (Shield 图标 + 同意按钮)
- **模态框2**: 候选人权益面板 (CandidateRightsPanel)

### Chat.tsx (286行)
- **职责**: 云试岗核心体验, 管理三幕场景 + 分岔沙盘 + 反向问答
- **核心数据**: `state.realityScenes`, `state.realityRoles`, `state.branchScenarios`, `state.trialSessions`
- **核心动作**: `completeRealityScene()`, `selectBranchChoice()`, `askReverseQuestion()`, `recordCandidateExitReason()`
- **新增**: `useChatStore().addUserMessage()` + `simulateAiStream()` — 触发右侧 Copilot 流式对话
- **UI 层次**:
  1. 毛玻璃 sticky header (backdrop-blur 14px)
  2. 安全信号标识 (⚡ 仅用于模拟体验)
  3. 三步进度指示器 (圆点 + 标签, 已完成=绿色对勾)
  4. 数字人角色卡片 (radial-gradient 背景 + 80px 渐变头像 + 呼吸环 shadow)
  5. 场景脚本卡片 (4px 角色色左边框 + 语音气泡三角指针)
  6. 操作按钮区 (2×2 Grid for branch choices, flex-wrap for scene actions)
  7. 反向问答室 (subtle 背景 + 类别图标按钮)
  8. 情绪反馈栏 (继续/有点难/换个方向)
  9. 完成庆祝卡片 (tint-green + scaleIn 动画)
  10. 退出原因弹窗 (2 列 Grid + blur 遮罩)

### Profile.tsx (约150行)
- **职责**: 对话式资料收集, 分段表单 + 进度指示
- **核心动作**: `submitCandidateApplicationWithAi()` → 生成报告
- **UI**: 数字人引导区 + 3 段表单(基本信息/我的背景/岗位相关) + 包容性按钮 + 进度条 + Loader2 提交按钮

### StoryPreview.tsx (约100行)
- **职责**: P3 报告预览, 65ch 编辑排版 + 证据悬停联动
- **新增**: `useChatStore().setActiveEvidenceId()` — 鼠标悬停触发右侧 ReviewPanel 切换
- **UI**: 报告头部(图标+标题+标签组) + 3 项能力亮点(左侧 2px 边框 + 证据锚点按钮) + CTA 按钮

### Success.tsx (约80行)
- **职责**: 投递成功确认, 五彩纸屑 + 成长建议 + 反向评估
- **UI**: 10 粒子 confetti 动画 + 96px 成功标记(pulseGlow) + 后续步骤时间线 + 能力亮点卡片(3 分类) + 面试准备器 + 高光时刻回放 + 滑块反向评估

---

## 三、核心组件详解

### 1. JobExplorationMap.tsx (约120行)
**定位**: P2 交互式岗位拓扑地图

**技术栈**: Framer Motion Spring + 极坐标数学 + CSS 3D

**交互流程**:
1. **入场**: 中心节点 scale(0→1) Spring 弹出, 7 个卫星节点从原点散射到轨道
2. **探索**: 点击节点 → 该节点高亮(accent 填充) + 其他节点 opacity→0.25 (聚光灯暗场)
3. **联动**: 通过 `chatStore.setExploreNodeId()` 触发右侧 ExplorePanel 切换情报卡片

**节点配置**: 8 个节点(core + 7 satellites), 每节点有 id/label/icon/angle/content
- core: 岗位核心 (112px, 12px 呼吸环 shadow)
- satellites: 技术栈/团队/工作节奏/成长路径/面试流程/薪资福利/岗位挑战 (56px)

**数学公式**: `angle = (i/N) * 2π`, `X = cos(θ) * 220px`, `Y = sin(θ) * 220px`

**动画参数**: `damping=15, stiffness=120, delay=0.1 + i*0.05s`

### 2. CopilotSidebar.tsx (约50行)
**定位**: 右侧 1/3 AI 副驾驶面板外壳

**架构**: 从 `useLocation().pathname` 派生模式 (零闪烁), 3 个面板同时挂载, 仅当前可见

**模式派生**:
```typescript
function deriveMode(pathname: string) {
  if (pathname.includes('/job/')) return 'explore';
  if (pathname.includes('/chat/')) return 'chat';
  return 'review';
}
```

**面板切换**: CSS `visibility: hidden` (无障碍, 从 DOM focus 移除) + `opacity` 过渡

### 3. ExplorePanel.tsx (约70行)
**定位**: 探索期 — "导游与情报官"

**交互**: 读取 `chatStore.exploreNodeId`, 通过 `AnimatePresence mode="wait"` 切换空状态/情报卡片

**情报数据库**: 8 条静态 Mock 数据, 每条含 title/content/badges[]. "针对这点提问" 按钮预留交互入口

### 4. ChatPanel.tsx (约80行)
**定位**: 试岗期 — "面试官与教练"

**核心功能**:
- 智能滚动: 50px 缓冲容差, 仅当用户处于底部时自动跟随
- 流式渲染: `activeStreamContent` 逐字追加 + 闪烁光标 `|` 动画
- 思考状态: `typing-dots` 3 个弹跳圆点 (animation-delay 错开)
- 悬浮按钮: 用户向上滚动时显示 "↓ 有新消息"

**数据源**: `useChatStore()` — chatHistory / activeStreamContent / isAiTyping

### 5. ReviewPanel.tsx (约90行)
**定位**: 复盘期 — "评估者与顾问"

**双模式**:
- **默认**: 面试准备器 (AI 模拟提问预判 + 建议反问 HR 的问题)
- **高亮**: 证据回放器 (Spring scale 切换, 显示对话切片气泡)

**联动**: 读取 `chatStore.activeEvidenceId`, 通过 `AnimatePresence` 切换面板

### 6. ComplianceNotice.tsx (约35行)
- 可关闭合规通知横幅
- Shield 图标 + AI 披露文字 + X 关闭按钮
- `useState(false)` 控制显示/隐藏

---

## 四、状态管理 (双 Store 架构)

### demoStore.ts (约1300行, 未改动)
**职责**: 业务数据持久化

| 管理内容 | 示例 |
|---------|------|
| 岗位数据 | jobs, avatars, realityRoles, realityScenes, jobTruthLabels, jobTruthContracts, branchScenarios |
| 候选人数据 | candidates, conversations, trialSessions, storyCards |
| 报告数据 | realityReports, trustAuditEvents, trustRepairTasks |
| 指标数据 | metrics (25+ 字段) |
| 操作方法 | addJob, startTrialSession, submitCandidateApplication, inviteCandidate, markReportReviewed 等 20+ |

**持久化**: localStorage + CustomEvent 跨标签页同步

### chatStore.ts (约70行, Zustand)
**职责**: 高频流式 UI 状态

| 状态 | 类型 | 说明 |
|------|------|------|
| chatHistory | Message[] | 对话历史 |
| activeStreamContent | string \| null | 当前正在流式输出的内容 (逐字追加, 不触发热数组重排) |
| isAiTyping | boolean | AI 是否正在打字 |
| exploreNodeId | string \| null | 探索地图当前选中节点 |
| activeEvidenceId | string \| null | 报告页当前高亮的证据 ID |

| 动作 | 说明 |
|------|------|
| addUserMessage(content) | 追加用户消息 |
| simulateAiStream(fullResponse) | Mock 流式输出 (600ms 延迟 + 15-40ms 随机字符间隔) |
| setExploreNodeId(id) | 设置探索节点 → 触发右侧情报切换 |
| setActiveEvidenceId(id) | 设置证据高亮 → 触发右侧回放切换 |

---

## 五、状态联动矩阵

```
┌─────────────────┬──────────────────┬─────────────────────┐
│ 左侧触发         │ chatStore 信号    │ 右侧响应             │
├─────────────────┼──────────────────┼─────────────────────┤
│ 点击地图节点     │ setExploreNodeId │ ExplorePanel 情报卡片│
│ 悬停证据按钮     │ setActiveEvidence│ ReviewPanel 对话回放 │
│ 选择分支/反问    │ addUserMessage   │ ChatPanel 流式打字   │
│                 │ +simulateAiStream│                     │
│ 页面路由切换     │ deriveMode()     │ 面板 opacity 切换    │
└─────────────────┴──────────────────┴─────────────────────┘
```

---

## 六、数据流

```
JobDetail.tsx
  └─ startTrialSession(jobId)
      └─ 导航 → Chat.tsx (sessionId)
          └─ completeRealityScene / selectBranchChoice / askReverseQuestion
              └─ 导航 → Profile.tsx
                  └─ submitCandidateApplicationWithAi(jobId, profile, draftId, sessionId)
                      ├─ 同步: generateRealityReport() — 19步链式生成
                      └─ 异步: DeepSeek API → adaptAiReportToRealityReport()
                          └─ 导航 → StoryPreview.tsx
                              └─ 导航 → Success.tsx
```

所有业务数据变更通过 `demoStore.updateDemoState()` → `saveDemoState()` → localStorage 持久化.

---

## 七、CSS 设计系统

### 双主题

| 主题 | 触发 | 特点 |
|------|------|------|
| Candidate | `data-theme="candidate"` | 温暖奶油 `#FAF8F6` + 鼠尾草绿 `#3D4F47` |
| HR | `data-theme="hr"` | 深炭色 `#16181D` + 暖金 `#C9A96E` |

### 布局系统

| 类名 | 用途 |
|------|------|
| `.reality-pro-workspace` | 全宽 Grid 容器 (1fr 400px) |
| `.workspace-main-stage` | 左侧主舞台 (100vh, overflow-y: auto) |
| `.workspace-copilot-panel` | 右侧 Copilot (100vh, sticky header) |
| `.copilot-header` | 小遇头像 + 名称 (sticky) |
| `.copilot-dynamic-content` | 面板切换区 (相对定位) |
| `.copilot-panel` | 单个面板 (绝对定位, visibility 切换) |

### 对话系统

| 类名 | 用途 |
|------|------|
| `.msg-row` / `.msg-them` / `.msg-me` | 消息行 (fadeInUp 动画) |
| `.chat-avatar` / `.avatar-hr` / `.avatar-teammate` / `.avatar-manager` | 数字人头像 (角色渐变色) |
| `.chat-bubble` | 聊天气泡 |
| `.typing-dots` | 输入指示器 (3 圆点弹跳) |
| `.quick-replies` / `.quick-reply` | 快速回复按钮 |

### 视觉原语

| 类名 | 用途 |
|------|------|
| `.panel-accent` | 4px 彩色左边框面板 |
| `.panel-tinted` | 5% 着色背景面板 |
| `.panel-gradient` | 3px 渐变顶部线面板 |
| `.card-lift` | 悬停上浮 + 阴影 |
| `.metric-ring` | CSS 圆锥渐变圆环 |
| `.pulse-ring` | 脉冲发光动画 |
| `.animate-in` | 交错子元素入场 |
| `.section-accent-bar` | 3px 渐变分隔线 |
| `.glass-panel` | 毛玻璃效果 |
| `.hero-gradient` | 径向渐变英雄背景 |

### 字体
```css
--font-sans: "DM Sans", "Noto Sans SC", ...;
--font-display: "DM Serif Display", "Noto Serif SC", ...;
--font-mono: "JetBrains Mono", ...;
```

### 动画 (9 个关键帧)
`fadeInUp` | `fadeIn` | `scaleIn` | `shimmer` | `pulseGlow` | `slideRight` | `confettiDrop` | `countPop` | `spin` | `typingBounce`

### 响应式

| 断点 | 布局变化 |
|------|---------|
| ≤640px | 单列, 2 列 KPI, 分支单列 |
| ≤960px | 侧边栏→底部抽屉, 3 列 KPI |
| ≥1200px | 6 列元数据, 4 列统计 |

---

## 八、技术依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| react | 19.2 | UI 框架 |
| react-router-dom | 7.9 | 路由 |
| framer-motion | latest | 弹簧动画 + AnimatePresence |
| zustand | latest | chatStore 流式状态 |
| lucide-react | latest | 图标库 |
| recharts | latest | 分析图表 (HR 端) |
| vite | 7.1 | 构建工具 |
| typescript | 5.9 | 类型系统 |

---

## 九、P0→P3 架构演进总结

| 阶段 | 核心交付 | 关键技术 |
|------|---------|---------|
| P0 | chatStore + ChatPanel 流式打字 | Zustand + activeStreamContent + setTimeout 模拟 |
| P1 | Chat 云试岗 + 分支选择联动 | useChatStore.getState() + simulateAiStream |
| P2 | 探索地图 + 弹簧散射 | Framer Motion Spring + 极坐标 + 聚光灯暗场 |
| P3 | 报告复盘 + 证据溯源 | 65ch 编辑排版 + AnimatePresence + 悬停联动 |

**全链路闭环**: 探索(P2) → 试岗(P1) → 复盘(P3) — 左侧主舞台 + 右侧 Copilot 双端联动, 数字人全程陪伴.

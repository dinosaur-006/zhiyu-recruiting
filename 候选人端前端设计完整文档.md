# 候选人端前端设计完整文档

> **产品**: 职遇 Reality Pro — AI招聘信任治理系统
> **设计系统**: Swiss Spa（候选人）/ Dark Editorial（HR）
> **字体**: DM Sans（正文）+ DM Serif Display（标题）+ Noto Serif SC（中文衬线）
> **图标库**: lucide-react（strokeWidth=1.5）
> **技术栈**: React 19 + TypeScript 5.9 + Vite 7 + Recharts

---

## 一、整体架构

候选人端共 **5 个页面 + 1 个核心组件**，通过 React Router 路由串联，共享 `demoStore` 数据层：

| 路由 | 页面 | 功能定位 |
|---|---|---|
| `/candidate/job/:jobId` | JobDetail | 入职门户 — 岗位探索地图 |
| `/candidate/chat/:jobId` | Chat | 核心体验 — 自适应云试岗 |
| `/candidate/profile/:jobId` | Profile | 信息收集 — 对话式技能发现 |
| `/candidate/story/:candidateId` | StoryPreview | 报告预览 — AI 洞察卡片 |
| `/candidate/success/:candidateId` | Success | 完成确认 — 个性化总结+反向评估 |

**数据流**: 全部页面通过 `useDemoState()` hook 读取共享 store，通过 demoStore 导出的 action 函数写入（`startTrialSession`, `submitCandidateApplicationWithAi` 等），数据持久化到 localStorage。

---

## 二、页面详细设计

### 2.1 JobDetail — 岗位探索地图

**功能**: 候选人的入职门户。不采用传统"JD 文档"式布局，而是通过**交互式可视化地图**让候选人自由探索岗位的 8 个方面。

#### 排版布局

```
┌──────────────────────────────────────────────┐
│  ← 返回        职遇 Reality        直接投递   │  顶栏（简洁三区）
├──────────────────────────────────────────────┤
│                                              │
│              ┌─────────┐                     │
│         ┌────│  团队   │────┐                │
│         │    └─────────┘    │                │  7 个外围节点
│    ┌────┴───┐          ┌───┴─────┐           │  放射状排列
│    │ 技术栈  │          │ 工作节奏 │           │
│    └────┬───┘          └───┬─────┘           │
│         │                  │                 │
│         │    ┌────────┐    │                 │
│         └────│ 岗位核心│────┘                 │  中心节点
│              │ 前端开发│                      │  (72px, 带阴影)
│              │ 20k-35k│                      │
│              └───┬────┘                      │
│    ┌─────────┐   │    ┌─────────┐           │
│    │ 面试流程 │───┼────│ 薪资福利 │           │
│    └─────────┘   │    └─────────┘           │
│              ┌───┴───┐                       │
│              │岗位挑战│                       │
│              └───────┘                       │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │ 👤 小遇 · AI体验官                     │    │  数字人向导栏
│  │ "你好像对技术栈特别感兴趣..."          │    │  (accent 背景)
│  │                        [调整我的关注]  │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  已探索 5/8 ████████░░   [进入云试岗 →]     │  进度+动作
└──────────────────────────────────────────────┘
```

#### 组件设计

**地图区域** (380px 高, 圆角 16px, 相对定位容器)
- **SVG 连接线层**: 绝对定位覆盖整个区域, 线条连接相关节点. 已探索的线: `var(--color-accent)` + 透明度 0.6 + 线宽 2px. 未探索: `var(--color-border)` + 透明度 0.3 + 线宽 1px. 使用 CSS transition 实现状态切换动画
- **8 个交互节点**: 绝对定位, 圆形按钮. 中心节点 72px（带 `--shadow-md` 阴影 + z-index 2）, 外围节点 52px. 已探索: `var(--color-accent)` 实心 + 白色图标. 未探索: 白色背景 + `var(--color-border)` 2px 边框 + muted 图标. 每个节点下方有文字标签(10-13px). 使用 CSS transition 0.2s 实现状态切换

**展开内容卡片**: 当节点被点击时, 在地图下方以 `fadeInUp 0.3s` 动画展示. 包含标题(16px bold) + 正文(14px, muted, 1.7行高)

**数字人向导栏**: flex 行布局, 40px 圆形头像(accent 背景+白色 User 图标) + 消息文本. 右侧"调整我的关注"按钮(11px, 无背景). 使用前端加权规则（非API调用）: 根据点击节点ID触发预设引导语

**底部进度+动作**: flex 行, 左侧显示"已探索 X/8" + 120px 进度条, 右侧"进入云试岗"按钮. 按钮初始禁用(灰色, `cursor: not-allowed`), 探索 ≥4 个节点后激活(accent 色, `cursor: pointer`)

#### 8 个探索节点

| 节点 ID | 标签 | 图标 | 关联节点 | 内容来源 |
|---------|------|------|---------|---------|
| core | 岗位核心 | Briefcase | 全部7个 | job.title, department, location, salary, experience |
| tech | 技术栈 | Code | core, challenge | job.analysis.hardSkills |
| team | 团队 | Users | core, growth | job.teamInfo |
| pace | 工作节奏 | Clock | core, salary | truthLabel (workPace, overtimeVolatility, collaborationDensity, pressureSources) |
| growth | 成长路径 | TrendingUp | core, team | truthLabel (growthSpeed, autonomy) + job.growthPath |
| interview | 面试流程 | Shield | core, salary | job.interviewProcess |
| salary | 薪资福利 | Coins | core, interview, pace | job.salaryMin-Max + 说明文字 |
| challenge | 岗位挑战 | Swords | core, tech | job.challenges |

#### 弹窗组件

**AI 身份披露弹窗** (consent-modal):
- 居中浮层, 圆角 16px, 白色背景, `--shadow-lg`
- 顶部 Shield SVG 图标 (48px, accent 色填充, 带 checkmark 路径)
- "开始前请确认" eyebrow + "AI身份披露" 标题
- 说明文字(14px, muted, 1.7行高)
- 两个按钮: "同意并开始云试岗"(primary) + "直接投递简历"(ghost)

**候选人权益弹窗** (consent-modal wide):
- 同样是居中浮层, max-width 680px
- 内嵌 `CandidateRightsPanel` 组件

---

### 2.2 Chat — 自适应云试岗

**功能**: 核心体验 — 候选人通过数字人引导完成 3 幕场景体验、分岔任务沙盘和反向问答. 接收 JobDetail 传递的 sessionId 管理完整试岗状态.

#### 排版布局

```
┌──────────────────────────────────────────────┐
│  [←] 岗位实境舱  第1幕/3幕·概览·前端开发    │  粘性顶栏
│                     [暂不继续] [直接投递]     │
├──────────────────────────────────────────────┤
│  ⚡ 当前对话仅用于模拟体验                    │  安全信号标识
├──────────────────────────────────────────────┤
│         ●━━○━━○                              │  三步进度
│       概览  日常  挑战                        │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │  (渐变背景)                           │    │  数字人头像区
│  │   [HR头像] 小遇 · HR数字人             │    │  (radial-gradient)
│  │   负责岗位概览、流程说明               │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │ ◣ 你好！我是这个岗位的AI体验官...     │    │  场景脚本卡片
│  │    (4px accent 左边框 + 三角指针)     │    │  (语音气泡风格)
│  └──────────────────────────────────────┘    │
│                                              │
│  [了解岗位职责] [了解面试流程] [直接投递]    │  操作按钮
│                                              │
│  ┌──────────────┐  ┌──────────────┐         │
│  │ (A) 等接口   │  │ (B) Mock先行 │         │  分岔选择
│  │ 稳定后再开发 │  │ 推进前端结构 │         │  (2×2 网格)
│  └──────────────┘  └──────────────┘         │
│  ┌──────────────┐  ┌──────────────┐         │
│  │ (C) 静态页面 │  │ (D) 确认优先级│         │
│  └──────────────┘  └──────────────┘         │
│                                              │
│  ⏰ 工作节奏  💰 薪资福利  👥 团队氛围      │  反向问答室
│  📈 成长空间  🎯 岗位挑战  📅 面试流程      │  (带类别图标)
│                                              │
│  情绪反馈:  [😊 继续] [🤔 有点难] [🔄 换个方向] │
└──────────────────────────────────────────────┘
```

#### 组件设计

**粘性顶栏** (sticky, z-index 30, `--shadow-sm`):
- 左侧: 36px 圆形角色头像(chat-avatar, 按角色变色) + "岗位实境舱" + 进度文字
- 右侧: "暂不继续"(ghost tiny) + "直接投递"(ghost tiny)

**安全信号标识**: 对话区顶部, 12px muted 文字, `⚡ 当前对话仅用于模拟体验，不会直接影响你的应聘结果`

**三步进度指示器**: flex 行, 居中, 3 个圆形步骤 + 标签. 已完成: 绿色对勾(Check size=14), 活跃: accent 色填充 + pulse-ring 动画, 未完成: subtle 色 + border

**数字头像区**: radial-gradient 背景(accent-subtle → transparent), 圆角 lg, 内含 80px 圆形头像 + 姓名/角色/职责文字. 头像使用 `linear-gradient` 按角色变色: HR=海军蓝渐变, 同事=绿色渐变, 主管=琥珀色渐变

**场景脚本卡片**: 白色背景 + 1px border + 4px accent 色左边框(随角色变化) + `--shadow-sm`. 左侧三角指针(`::before` 伪元素, 8px 三角形, 颜色匹配边框). 内容: 标题 eyebrow + 正文(16px, 1.75行高)

**操作按钮**: ghost-button 样式, flex wrap 排列, gap 8px

**分岔选择卡片**: 2×2 CSS Grid. 每个选项: card-lift hover 效果, 28px 圆形 choice-label(未选: border + muted; 已选: accent 填充 + 白色 + `scaleIn` 动画). 已选卡片: `panel-tinted tint-green`

**反向问答室**: 灰色背景面板(subtle), 圆角 lg. 问题按钮带 lucide 类别图标(⏰ Clock, 💰 DollarSign, 👥 Users, 📈 TrendingUp, 🎯 Target, 📅 Calendar). 已提问的答案以卡片形式展示

**情绪反馈栏**: 三个小按钮, flex 行: "😊 继续"(ghost), "🤔 有点难"(ghost), "🔄 换个方向"(ghost). "换个方向"点击后显示过渡消息, 保持同能力锚点变换语境包装

**完成反馈**: 绿色着色面板(tint-green + `scaleIn` 动画), 居中文字 + "补充云试岗资料" primary 按钮

**退出原因弹窗**: 2 列网格, 7 个退出原因选项(ghost 按钮). 底部"继续云试岗" primary 按钮

#### 三个数字人角色

| 角色 | 类型 | 头像渐变色 | 左边框色 | 语气 |
|------|------|-----------|---------|------|
| HR数字人 小遇 | hr | `#1E3F66 → #3A6B99` | `#1E3F66` | 专业、透明、友好 |
| 未来同事 林同学 | teammate | `#3D7A62 → #5BAA80` | `#3D7A62` | 自然、真实、有亲和力 |
| 未来主管 周主管 | manager | `#A67F44 → #D4A84B` | `#A67F44` | 直接、清晰、重视问题解决 |

---

### 2.3 Profile — 对话式技能发现

**功能**: 候选人补充个人资料. 采用**对话式引导**替代传统 Web 表单——数字人引导 + 分段式输入 + 进度指示.

#### 排版布局

```
┌──────────────────────────────────────────────┐
│  ← 返回                      [蓝色/绿色模式徽章]│
├──────────────────────────────────────────────┤
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │ 👤 小遇                               │    │
│  │ "让我再了解你一点信息。你可以简单     │    │
│  │  聊聊，也可以详细展开，按你的节奏来。"│    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ─── 基本信息 ────────────────────────────   │
│  ┌─────────────┐  ┌─────────────┐           │
│  │ 姓名        │  │ 手机号       │           │  2 列网格
│  │ [________]  │  │ [________]  │           │
│  └─────────────┘  └─────────────┘           │
│  ┌─────────────┐  ┌─────────────┐           │
│  │ 邮箱        │  │ 来源渠道     │           │
│  │ [________]  │  │ [________]  │           │
│  └─────────────┘  └─────────────┘           │
│                                              │
│  ─── 我的背景 ────────────────────────────   │
│  ┌──────────────────────────────────────┐    │
│  │ 技能标签                              │    │
│  │ [____________________________]        │    │
│  └──────────────────────────────────────┘    │
│  ┌──────────────────────────────────────┐    │
│  │ 项目经历                              │    │
│  │ [____________________________]        │    │
│  │ 已输入 42 字                           │    │  字符计数
│  └──────────────────────────────────────┘    │
│                                              │
│  [给你更多时间]                               │  包容性按钮
│                                              │
│  已填写 6/8 ████████░░                       │  进度条
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │        🔄 正在生成报告...              │    │  提交按钮
│  └──────────────────────────────────────┘    │
│                                              │
│  ← 返回岗位实境舱入口                        │
└──────────────────────────────────────────────┘
```

#### 组件设计

**模式徽章**: 顶栏下方, 蓝色 = "直接投递模式", 绿色 = "云试岗补充模式", 使用 Badge 组件 + badge-dot

**数字人引导区**: 40px 圆形头像 + 气泡式文字, 背景 accent-subtle, 圆角 14px

**表单分组** (3 个 section-accent-bar 分隔):
1. **基本信息** (panel-gradient 顶部): 姓名/手机/邮箱/来源渠道 — 2 列网格
2. **我的背景**: 技能标签/项目经历/求职动机 — 全宽字段
3. **岗位相关**: 关注点/节奏接受度/补充说明/场景反思 — 全宽 textarea

**字段组件**: label(13px, muted) + input/textarea(白色背景, border, 圆角 8px, focus 时 border accent + `0 0 0 3px accent-subtle` 阴影). textarea 标签旁显示字符计数(11px, muted)

**包容性按钮**: "给你更多时间" ghost tiny 按钮, 点击后显示安抚文字

**进度指示器**: "已填写 X/8" + 120px 进度条(`--color-accent` 填充)

**提交按钮**: full-width primary, 生成中显示 `Loader2` 旋转图标(spin 动画) + "正在生成报告..." 文字, 外围 pulseGlow 动画

---

### 2.4 StoryPreview — AI 洞察预览

**功能**: 候选人查看自己的云试岗报告预览. 每条洞察附带行为证据锚点.

#### 排版布局

```
┌──────────────────────────────────────────────┐
│  ┌──────────────────────────────────────┐    │
│  │ ℹ️ HR 将看到以下由 AI 辅助生成的       │    │  信息横幅
│  │   报告内容。每条洞察都附有行为证据锚点。│    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ─── 我关注的岗位信息 ────────────────────   │
│  成长空间、团队技术氛围                        │
│                                              │
│  ─── 我看过的岗位真相点 ──────────────────   │
│  [工作节奏] [成长速度] [协作密度]              │  Badge 标签
│                                              │
│  ─── 你的能力亮点 ────────────────────────   │
│  ┌──────────────────────────────────────┐    │
│  │ ✅ 主动推进型协作                      │    │
│  │    证据：在分岔任务中选择了 Mock 先行 │    │  洞察卡片
│  │    推进方案，并同步产品确认优先级     │    │  (panel-accent)
│  └──────────────────────────────────────┘    │
│                                              │
│  ─── 你可以反问 HR 的好问题 ──────────────   │
│  1. Code Review 和新人培养机制是怎样的？      │
│  2. 项目节点压力是否常态化？                 │
│  3. 薪资沟通在哪个阶段进行？                 │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │         确认投递                       │    │  主按钮(pulse-ring)
│  └──────────────────────────────────────┘    │
│  [重新体验云试岗]    [编辑资料]               │  辅助按钮
└──────────────────────────────────────────────┘
```

#### 组件设计

**信息横幅**: accent-subtle 背景, Shield 图标, 说明文字(13px)

**报告分区**: section-accent-bar 分隔线划分 4-5 个逻辑区域. story-tags 显示为 Badge 标签行. story-block 显示为标题+正文

**洞察卡片**: panel-accent 左边框(彩色, 匹配洞察类型) + 标题(bold) + 证据锚点文字(muted italic). 每条洞察附带 `badge-dot` 状态标识(绿色=有证据, 琥珀色=需确认)

**反问 HR 问题列表**: 编号列表, 13px 文字, 1.6 行高

**操作按钮**: "确认投递"(primary + pulse-ring 动画), "重新体验云试岗"(ghost + card-lift), "编辑资料"(ghost)

---

### 2.5 Success — 个性化总结 + 反向评估

**功能**: 投递完成确认. 包含五彩纸屑动画、能力洞察、面试准备器、反向评估.

#### 排版布局

```
┌──────────────────────────────────────────────┐
│  🎊 五彩纸屑覆盖层 (10 个粒子, 2.8s 循环)     │  固定定位, 不阻挡交互
│                                              │
│       ┌────────────────────┐                 │
│       │        ✅           │                 │  96px 圆形
│       │     (48px 图标)     │                 │  pulseGlow + scaleIn
│       └────────────────────┘                 │
│                                              │
│            [投递成功]                         │  green Badge
│                                              │
│       你的云试岗记录已完成                    │  h1 标题
│                                              │
│  ─── 后续步骤 ────────────────────────────   │
│  ①  HR 审阅你的云试岗报告                    │  编号时间线
│  ②  HR 可能会发送信任修复消息               │
│  ③  面试邀请                                 │
│                                              │
│  ─── 能力亮点与成长建议 ──────────────────   │
│  ┌──────────────────────────────────────┐    │
│  │ 🟢 本岗位相关                         │    │
│  │    • 主动推进复杂任务的能力           │    │
│  │    • 跨部门协作沟通意识               │    │
│  │ 🔵 跨岗位通用                         │    │
│  │    • 结构化问题拆解思维               │    │
│  │ 🟡 发展型 (未来6个月)                  │    │
│  │    • 技术决策的深度和广度             │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ─── 面试准备器 ──────────────────────────   │
│  定制的模拟面试题:                            │
│  • "请描述一次你在需求不明确的情况下..."      │
│  你可以反问 HR 的好问题:                      │
│  • "团队的 Code Review 机制是怎样的？"       │
│                                              │
│  ─── 你的高光时刻 ────────────────────────   │
│  ┌──────────────────────────────────────┐    │
│  │ 小遇："如果上线时间不变，你怎么推进？" │    │
│  │ 你："我会先拆必做和可延期功能..."      │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ─── 体验反馈 ──────────────────────────   │
│  尊重度:    ○○○○●  (4/5)                    │  滑块评分
│  流畅度:    ○○○●●  (3/5)                    │
│  自然度:    ○○○○○  (5/5)                    │
│  📝 你的反馈仅用于产品改进, 不与个人档案关联  │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │         HR查看云试岗报告               │    │
│  └──────────────────────────────────────┘    │
│         [返回演示入口]                        │
└──────────────────────────────────────────────┘
```

#### 组件设计

**五彩纸屑覆盖层**: 固定定位, inset 0, pointer-events none. 10 个粒子 div, 绝对定位, 不同 left 位置(8%-88%), 不同 animation-delay(0-0.65s), 不同颜色(使用 CSS 变量: positive, accent, warning, info). 粒子大小 8-14px, 部分为圆形(border-radius 50%). `confettiDrop` 关键帧: translateY(-60px→100vh) + rotate(0→720deg)

**成功标记**: 96px 圆形, 绿色 gradient ring, CircleCheckBig size=48(白色), pulseGlow 动画. scaleIn 入场动画

**后续步骤时间线**: 3 个编号步骤, 每个步骤带 CheckCircle2 图标覆盖. subtle 背景面板, 圆角 8px

**能力亮点卡片**: 3 个分类区域, 各带彩色圆点标识(绿色=本岗位, 蓝色=跨岗位, 琥珀色=发展型). 每个分类下列出 2-3 个要点

**面试准备器**: 2 个子区域: "定制的模拟面试题" + "你可以反问 HR 的好问题". 编号列表, 13px 文字

**高光时刻**: 对话切片以聊天气泡形式回放. 灰色背景, 显示小遇的问题 + 候选人的回答

**反向评估**: 3 个滑块评分(尊重度/流畅度/自然度, 1-5 分). 5 个圆形"星"按钮, 点击切换填充. 底部文字标注匿名+仅用于产品改进

**操作按钮**: "HR查看云试岗报告"(primary, full-width) + "返回演示入口"(ghost, full-width)

---

## 三、布局外壳 — CandidateLayout

**功能**: 极简布局容器, 仅负责设置温暖色主题 + 渲染子路由.

```typescript
export function CandidateLayout() {
  useEffect(() => {
    document.documentElement.dataset.theme = 'candidate';
  }, []);
  return (
    <div className="candidate-shell">
      <Outlet />
    </div>
  );
}
```

- `candidate-shell`: min-height 100vh, `var(--color-base)` 背景 (`#FAF8F6` 温暖奶油色)
- 不设顶栏/侧栏 — 每个页面自行管理头部导航
- `data-theme="candidate"` 触发温暖 Swiss Spa 配色方案

---

## 四、CSS 设计变量

### 颜色（候选人主题）

| Token | Value | 用途 |
|-------|-------|------|
| `--color-base` | `#FAF8F6` | 页面背景（温暖奶油色） |
| `--color-surface` | `#FFFFFF` | 卡片/面板表面 |
| `--color-accent` | `#3D4F47` | 强调色（鼠尾草绿） |
| `--color-positive` | `#3D7A62` | 成功/已完成 |
| `--color-warning` | `#A67F44` | 警告/待处理 |
| `--color-negative` | `#B84548` | 危险/高优先级 |
| `--color-info` | `#4F6D7A` | 信息/中性提示 |

### 间距（8px 基础刻度）

`--space-1`(4) `--space-2`(8) `--space-3`(12) `--space-4`(16) `--space-5`(20) `--space-6`(24) `--space-8`(32) `--space-10`(40) `--space-12`(48)

### 字体

```css
--font-sans: "DM Sans", "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
--font-display: "DM Serif Display", "Noto Serif SC", "PingFang SC", serif;
--font-mono: "JetBrains Mono", "SF Mono", "Cascadia Code", monospace;
```

h1-h4 使用 `var(--font-display)`（衬线标题）, 正文使用 `var(--font-sans)`.

### 关键 CSS 类

| 类名 | 用途 |
|------|------|
| `.conversation-shell` | 对话式布局容器(max-width 900px) |
| `.msg-row` / `.msg-them` / `.msg-me` | 消息行(带头像), fadeInUp 入场 |
| `.chat-avatar` / `.avatar-hr` / `.avatar-teammate` / `.avatar-manager` | 数字人头像(按角色渐变色) |
| `.chat-bubble` | 聊天气泡(对方浅色, 自己 accent 色) |
| `.chat-rich-card` | 对话内嵌信息卡片 |
| `.quick-replies` / `.quick-reply` | 快速回复按钮(药丸形) |
| `.typing-dots` | "正在输入"动画(3 个弹跳圆点) |
| `.chat-form-field` | 对话内嵌表单输入框 |
| `.panel-accent` | 带 4px 彩色左边框的面板 |
| `.panel-tinted` | 带 5% 着色背景的面板 |
| `.panel-gradient` | 带 3px 渐变顶部线的面板 |
| `.card-lift` | 悬停时上浮 + 阴影 |
| `.metric-ring` | CSS 圆锥渐变圆环(0-100 分数可视化) |
| `.hero-gradient` | 径向渐变英雄背景 |
| `.glass-panel` | 毛玻璃效果(backdrop-blur) |
| `.pulse-ring` | 脉冲发光动画 |
| `.animate-in` | 交错子元素 fadeInUp(每 80ms 延迟) |
| `.section-accent-bar` | 3px 渐变分隔线 |

---

## 五、响应式设计

| 断点 | 变化 |
|------|------|
| ≤640px | 单列布局, 2 列 KPI 网格, 分支选择单列 |
| ≤960px | 侧边栏折叠, 3 列 KPI, 分析双列变单列 |
| ≥1200px | 6 列元数据条, 4 列统计网格, 3 列编辑支柱 |

移动端: `prefers-reduced-motion` 查询关闭所有动画.

---

## 六、动画系统

### 关键帧

| 动画 | 时长 | 用途 |
|------|------|------|
| `fadeInUp` | 0.5s | 入场动画(translateY 16px → 0) |
| `fadeIn` | 0.5s | 简单透明度过渡 |
| `scaleIn` | 0.5s | 缩放入场(0.94 → 1) |
| `shimmer` | 1.5s | 骨架屏加载(background-position 扫描) |
| `pulseGlow` | 2s | 脉冲发光(按钮/头像) |
| `confettiDrop` | 2.8s | 五彩纸屑下落(translateY + rotate) |
| `typingBounce` | 1.2s | 输入指示器弹跳(translateY 0 → -6px) |
| `spin` | 0.6s | 旋转加载器 |
| `countPop` | 0.5s | 数字计数器弹跳(scale 1 → 1.08) |

---

## 七、数据流

```
JobDetail
  └─ startTrialSession(jobId) → 创建 TrialSession → 导航到 Chat
      └─ Chat: completeRealityScene / selectBranchChoice / askReverseQuestion
          └─ 完成后导航到 Profile
              └─ submitCandidateApplicationWithAi(jobId, profile, draftId, sessionId)
                  ├─ 同步: submitCandidateApplication → generateRealityReport (19步链)
                  └─ 异步: AI generateHrReport → adaptAiReportToRealityReport
                      └─ 导航到 StoryPreview
                          └─ 导航到 Success
```

所有状态变更通过 `updateDemoState()` → `saveDemoState()` → localStorage 持久化.
跨标签页同步通过 `CustomEvent('zhiyu-demo-store-updated')` 实现.
